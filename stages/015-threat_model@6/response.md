{
  "entryPoints": [
    "lib/sqlite3/statement.rb:35 — Statement#bind_params receives caller-supplied bind values (often request-derived); flattens arrays and treats Hash elements as named-parameter maps, so both the parameter names and the values are attacker-influenceable",
    "lib/sqlite3/statement.rb:61 — Statement#execute takes *bind_vars from application code and forwards them to bind_params before creating a ResultSet",
    "lib/sqlite3/statement.rb:85 — Statement#execute! same untrusted bind-var surface, plus a caller-supplied block",
    "lib/sqlite3/statement.rb:104 — Statement#each pulls rows out of the database via #step; every column value entering Ruby here is untrusted database content",
    "lib/sqlite3/statement.rb:124 — get_metadata ingests column_name/column_decltype strings from the database (schema-controlled, i.e. untrusted if any writer can create tables/views/aliases)",
    "lib/sqlite3/resultset.rb:41 — ResultSet#reset re-binds arbitrary caller-supplied bind_params onto an already-used statement",
    "lib/sqlite3/resultset.rb:65 — ResultSet#next is the main row-ingestion path: statement row values, @stmt.types and @stmt.columns all enter here from the database",
    "lib/sqlite3/translator.rb:37 — Translator#add_translator accepts an arbitrary application-supplied block keyed by type name",
    "lib/sqlite3/translator.rb:45 — Translator#translate is called with a database-controlled decltype string and a database-controlled value",
    "lib/sqlite3/translator.rb:53 — type_name receives the raw decltype string from the database and regex-matches/upcases it",
    "lib/sqlite3/value.rb:8 — Value#initialize takes a raw sqlite3_value handle plus a db; dead code today (no instantiation anywhere, and Database has no #driver), but the declared entry surface is a raw C handle",
    "ext/sqlite3/statement.c:31 — C Statement#initialize is the SQL-text entry point behind Database#prepare; SQL string arrives here from lib/sqlite3/database.rb:83",
    "ext/sqlite3/statement.c:192 — C bind_param is the real boundary for bind keys and values coming from lib/sqlite3/statement.rb:39-41",
    "ext/sqlite3/statement.c:106 — C step is where sqlite3 column bytes become Ruby objects for the whole component",
    "lib/sqlite3/pragmas.rb:220 — table_info interpolates a caller-supplied table name directly into 'PRAGMA table_info(#{table})' and prepares it; only non-database.rb caller of Statement"
  ],
  "sinks": [
    "ext/sqlite3/statement.c:53 — sqlite3_prepare_v2(db, StringValuePtr(sql), RSTRING_LEN(sql), &ctx->st, &tail): SQL compilation sink; the only place SQL text reaches the engine for this component",
    "ext/sqlite3/statement.c:64 — rb_str_new2(tail) for @remainder: NUL-terminated read of a pointer into the (possibly transcoded) sql buffer, no length and no encoding association",
    "ext/sqlite3/statement.c:211 — RSTRING_PTR(key)[0] read with no RSTRING_LEN check before indexing byte 0 of a bind-parameter name",
    "ext/sqlite3/statement.c:212 — sqlite3_bind_parameter_index(ctx->st, StringValuePtr(key)): NUL-terminated name lookup, so embedded NUL truncates the name that is matched",
    "ext/sqlite3/statement.c:215 — NUM2INT(key) converts a non-string bind key into a raw C int parameter index",
    "ext/sqlite3/statement.c:226 — rb_str_export_to_enc(value, enc) transcodes the bound string before the SQLite3::Blob class check at :230; the transcode result is a plain String",
    "ext/sqlite3/statement.c:231 — sqlite3_bind_blob(..., StringValuePtr(value), RSTRING_LEN(value), SQLITE_TRANSIENT)",
    "ext/sqlite3/statement.c:239 — sqlite3_bind_text(..., StringValuePtr(value), RSTRING_LEN(value), SQLITE_TRANSIENT)",
    "ext/sqlite3/statement.c:249 — sqlite3_bind_double(NUM2DBL(value))",
    "ext/sqlite3/statement.c:253 — NUM2LONG(value) into sqlite3_bind_int64; only T_FIXNUM reaches it, T_BIGNUM falls to the raise at :261",
    "ext/sqlite3/statement.c:127 — sqlite3_step(stmt): query execution sink",
    "ext/sqlite3/statement.c:145 — rb_tainted_str_new(sqlite3_column_text, sqlite3_column_bytes) + rb_enc_associate_index at :150: memory copy of engine bytes and encoding tagging without validity check",
    "ext/sqlite3/statement.c:157 — rb_tainted_str_new(sqlite3_column_blob, sqlite3_column_bytes): blob bytes become a plain String with no encoding and not wrapped in SQLite3::Blob",
    "ext/sqlite3/statement.c:138 — LONG2NUM(sqlite3_column_int64) on a 64-bit column value",
    "ext/sqlite3/statement.c:326 — rb_str_new2(sqlite3_column_name(...)) : NUL-terminated read, no length, no encoding association; feeds @columns",
    "ext/sqlite3/statement.c:342 — rb_str_new2(sqlite3_column_decltype(...)) : same, feeds @types and then the Translator",
    "ext/sqlite3/statement.c:85 — sqlite3_finalize(ctx->st) then ctx->st = NULL: resource-release sink reachable from ResultSet#close (lib/sqlite3/resultset.rb:104) and Database#prepare's ensure",
    "ext/sqlite3/statement.c:282 — sqlite3_reset(ctx->st) without sqlite3_clear_bindings, reachable from lib/sqlite3/resultset.rb:42 and lib/sqlite3/statement.rb:62",
    "ext/sqlite3/statement.c:12 — xfree(c) in the Data_Wrap_Struct free function; the wrapped sqlite3_stmt is not finalized here, only the struct is freed",
    "lib/sqlite3/translator.rb:66 — Time.parse(v) on database-controlled column text",
    "lib/sqlite3/translator.rb:68 — Date.parse(v) on database-controlled column text",
    "lib/sqlite3/translator.rb:69 — DateTime.parse(v) on database-controlled column text",
    "lib/sqlite3/translator.rb:89 — v.strip.gsub(/00+/,\"0\") plus repeated v.downcase on database-controlled column values for bit/bool/boolean",
    "lib/sqlite3/translator.rb:47 — @translators[...].call(type, value): dispatch into an arbitrary registered block, selected by a database-controlled type string",
    "lib/sqlite3/translator.rb:54 — @type_name_cache[type] ||= ...: unbounded cache keyed by database-controlled decltype strings (memory growth sink)",
    "lib/sqlite3/resultset.rb:76 — HashWithTypes[*@stmt.columns.zip(row).flatten]: builds a hash whose keys are database-controlled column names, then overwrites integer indices at :78",
    "lib/sqlite3/statement.rb:6 — SQLite3::Blob.new(self) via the String#to_blob monkey patch on ::String (global core-class modification)",
    "lib/sqlite3/pragmas.rb:220 — prepare \"PRAGMA table_info(#{table})\": SQL built by interpolation, not binding; Database.quote (lib/sqlite3/database.rb:45) is not applied"
  ],
  "assumptions": [
    "lib/sqlite3/statement.rb:37 — bind_params assumes flatten-then-positional is the caller's intent: a nested array of user input silently shifts positional indices, and a user-controlled Hash reaching this loop becomes named-parameter binding rather than one positional value",
    "lib/sqlite3/statement.rb:41 — assumes the number of supplied bind vars matches the statement's placeholders; nothing compares against bind_parameter_count (ext/sqlite3/statement.c:352), so under-binding leaves parameters NULL rather than erroring",
    "lib/sqlite3/statement.rb:62 — execute assumes active? (defined at :92 as !done?) means a previously used statement; a freshly prepared, never-stepped statement is also 'active', so reset! is always issued",
    "lib/sqlite3/statement.rb:100 — columns calls get_metadata without must_be_open!, unlike types at :116; assumes the statement is still open when column metadata is requested",
    "lib/sqlite3/statement.rb:107 — each assumes step returned a row iff done? is false; correctness depends entirely on ctx->done_p bookkeeping in ext/sqlite3/statement.c:174",
    "lib/sqlite3/resultset.rb:66 — ResultSet#next assumes the row returned by step is nil exactly when @stmt.done?, and it discards the row it just fetched when done? is true",
    "lib/sqlite3/resultset.rb:70 — assumes @stmt.types and the row have the same length; zip silently pads with nil when column_count and the row disagree",
    "lib/sqlite3/resultset.rb:76 — assumes column names are unique and non-nil; duplicate or nil names from the schema collapse hash keys, and integer keys written at :78 can collide with a column literally named as a number's key only via the same Hash",
    "lib/sqlite3/resultset.rb:82 — assumes the presence of #fields on an Array means the ArrayFields gem is loaded (feature detection by duck typing on a core class)",
    "lib/sqlite3/translator.rb:46 — translate assumes the value is a String for every registered translator; blob columns arrive as binary Strings (ext/sqlite3/statement.c:157) and integer/float columns arrive as Integer/Float (ext/sqlite3/statement.c:138,141), so Time.parse/strip/downcase assumptions are not enforced anywhere",
    "lib/sqlite3/translator.rb:56 — type_name assumes the decltype is a well-formed 'NAME(args)' string; anything before the first '(' becomes the lookup key, and no length or charset limit is applied",
    "lib/sqlite3/translator.rb:19 — the default translator proc assumes an unknown type should pass the value through unchanged, so a mis-declared column silently bypasses translation",
    "lib/sqlite3/value.rb:9 — assumes Database exposes a #driver object; no such method exists in lib/ or ext/, so this whole class assumes an API that was removed",
    "lib/sqlite3/value.rb:10 — assumes the caller passes a valid, still-live sqlite3_value handle; there is no ownership, lifetime, or type validation of the handle",
    "lib/sqlite3/value.rb:46 — type assumes value_type returns one of the five known constants; any other value falls through the case and returns nil",
    "ext/sqlite3/statement.c:150 — assumes the bytes SQLite returns are valid in the database encoding; the encoding index is associated, never validated or transcoded",
    "ext/sqlite3/statement.c:203 — bind_param assumes db.encoding is non-nil (rb_to_encoding unguarded), unlike initialize at :47 and step at :120 which fall back to UTF-8",
    "ext/sqlite3/statement.c:36 — Data_Get_Struct on the db argument assumes the caller passed a real SQLite3::Database; Statement.new is public and callable with any object of the right class only",
    "ext/sqlite3/statement.c:230 — assumes rb_str_export_to_enc preserves the SQLite3::Blob class, so the blob-vs-text decision assumes a transcode did not happen first",
    "lib/sqlite3/pragmas.rb:220 — assumes the table name given to table_info is trusted/validated by the caller; no quoting or identifier validation happens on this path"
  ],
  "trustBoundaries": [
    "lib/sqlite3/database.rb:83 -> ext/sqlite3/statement.c:31 — application SQL text crosses from Ruby into the C extension and then into the SQLite parser; this is the SQL-injection boundary for every Statement in the component",
    "lib/sqlite3/statement.rb:39-41 -> ext/sqlite3/statement.c:192 — bind keys and values cross from Ruby objects into C type switches, raw pointers, and integer conversions",
    "ext/sqlite3/statement.c:127-181 -> lib/sqlite3/resultset.rb:66 — the database-to-application boundary: engine-owned bytes become Ruby Strings/Integers/Floats and flow into application code",
    "ext/sqlite3/statement.c:326 and :342 -> lib/sqlite3/statement.rb:129-131 — schema-controlled column names and decltypes become the frozen @columns/@types arrays that the rest of the component trusts as metadata",
    "lib/sqlite3/statement.rb:130 -> lib/sqlite3/translator.rb:47 — a database-controlled decltype string selects which Ruby block executes over the row value; schema content therefore chooses code paths",
    "lib/sqlite3/resultset.rb:71 -> lib/sqlite3/translator.rb:66-69 — untrusted column text crosses into Time/Date/DateTime parsers",
    "lib/sqlite3/resultset.rb:76 -> application code — database-controlled column names become Hash keys handed to callers that typically index them by literal name",
    "ext/sqlite3/statement.c:119 and :203 -> Ruby #encoding — the C layer calls back into Ruby (rb_funcall) on every step and bind, so a Database subclass or singleton override of #encoding controls both output tagging and input transcoding of the extension",
    "lib/sqlite3/statement.rb:4-8 — String#to_blob monkey patch: the component adds a method to a global core class, so any object graph in the process crosses into SQLite3 blob semantics",
    "ext/sqlite3/database.c:86-87 -> lib/sqlite3/database.rb:53,57 — results_as_hash/type_translation come straight from the constructor options hash without booleanization and then steer ResultSet#next's shape and translation behavior",
    "lib/sqlite3/pragmas.rb:220 -> ext/sqlite3/statement.c:53 — a caller-supplied identifier crosses into compiled SQL by string interpolation, bypassing the bind-parameter boundary entirely"
  ],
  "hotFiles": [
    "lib/sqlite3/statement.rb",
    "lib/sqlite3/resultset.rb",
    "lib/sqlite3/translator.rb",
    "lib/sqlite3/value.rb",
    "ext/sqlite3/statement.c",
    "lib/sqlite3/database.rb",
    "ext/sqlite3/sqlite3_ruby.h",
    "ext/sqlite3/sqlite3.c",
    "lib/sqlite3/pragmas.rb"
  ]
}