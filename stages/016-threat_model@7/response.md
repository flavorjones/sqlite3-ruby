{"job_id":"threat:007-build-tasks-c7cb9cf0","artifact_key":"007-build-tasks-c7cb9cf0","component":"build-tasks","summary":"Build/install tooling for the sqlite3-ruby C extension: a small mkmf extconf, two rake task files (native compile via rake-compiler, and a vendoring task that downloads the SQLite amalgamation/DLL over plaintext HTTP and unzips it), and a vendored copy of Minero Aoki's setup.rb 3.3.0 installer. All of it runs at build/install time on the machine of whoever unpacks the gem or checks out the tree, frequently under make or sudo. Untrusted inputs are the developer/CI environment (ENV, ARGV), files inside the source tree that are eval'd or shell-interpolated (metaconfig, pre-/post-task hook scripts, .config, extension directory and file names), network-fetched archives (sqlite.org zips over http, then unzip into the tree), and host toolchain path lookups (wget/curl/unzip/swig/dlltool/make resolved via PATH). Trust boundaries are crossed when zip contents, config-file text, or tree-resident scripts become Ruby code or shell command strings.","entryPoints":["setup.rb:85 - ARGV scanned for `--rbconfig=PATH`; value passed straight to `require`, choosing a Ruby file to load before any validation exists.","setup.rb:698 - `parsearg_global` consumes ARGV to select the task name later dispatched via `__send__ \"parsearg_#{task}\"` / `__send__ \"exec_#{task}\"` (setup.rb:648-650).","setup.rb:741 - `parsearg_config` reads ARGV config options; a bare `-`/`--` copies the rest of ARGV verbatim into `@options['config-opt']`, later joined into a shell command line (setup.rb:1072-1074).","setup.rb:767 - `parsearg_install` reads `--prefix=` from ARGV into `@options['install-prefix']`, used as a filesystem path prefix for mkdir/install/chmod.","setup.rb:279 - `ConfigTable#initialize_from_file` parses the on-disk `.config` file (SAVE_FILE at setup.rb:293) with `line.split(/=/,2)`; values flow into `ruby-prog`, `make-prog`, `ruby-path` and all install directories with no key whitelist or validation.","setup.rb:305 - `ConfigTable#[]` recursively expands `$name` references out of the same untrusted table.","setup.rb:654 - `run_metaconfigs` reads `#{@ardir}/metaconfig` from the source tree; per-package variants at setup.rb:881-884.","setup.rb:608 - `run_hook` picks up `pre-/post-<task>` and `.rb` hook scripts from the directory currently being traversed (callers at setup.rb:1281,1287,1293 and 938-967).","setup.rb:1287 - `traverse` walks every subdirectory of bin/lib/ext/data; tree-supplied directory and file names become path components and command arguments.","setup.rb:1197 - `existfiles` collects names from both srcdir and objdir; those names reach `install`, `adjust_shebang` and `chmod` (setup.rb:1150-1165).","ext/sqlite3/extconf.rb:1 - `ENV['RC_ARCHS']` read/overwritten on darwin at extension-configure time.","ext/sqlite3/extconf.rb:7 - `ENV['CC']` copied into `RbConfig::MAKEFILE_CONFIG['CC']`; environment chooses the compiler written into the generated Makefile.","ext/sqlite3/extconf.rb:12 - `dir_config 'sqlite3', ...` accepts `--with-sqlite3-dir/-include/-lib` from the gem-install/extconf command line as include and library search paths.","Rakefile:26 - `ENV['FAT_DIR']` joined into the extension's `lib_dir` output path with no validation.","tasks/vendor_sqlite3.rake:36 - remote input: `http://www.sqlite.org/sqlite-amalgamation-<version>.zip` fetched over cleartext HTTP; body becomes headers/source compiled into the extension.","tasks/vendor_sqlite3.rake:46 - remote input: `http://www.sqlite.org/sqlitedll-<version>.zip` fetched over cleartext HTTP; contents become the DLL shipped in cross-built Windows gems.","tasks/vendor_sqlite3.rake:9 - `Rake::ExtensionCompiler.mingw_gcc_executable` / `mingw_host`: externally configured toolchain locations interpolated into a shell command string."],"sinks":["setup.rb:470 - `command(str)` executes `system str` as a single string, i.e. full shell interpretation; every build step funnels through here (setup.rb:472).","setup.rb:475 - `ruby(str)` = `command config('ruby-prog') + ' ' + str`; interpreter path comes from `.config` or CLI.","setup.rb:479 - `make(task)` = `command config('make-prog') + ' ' + task`; `make-prog` derived from `configure_args` (setup.rb:141-145) or `.config`.","setup.rb:1072 - `extconf` builds `\"#{config('ruby-prog')} #{curr_srcdir()}/extconf.rb #{opt}\"` and shells out; interpreter and `config-opt` are unquoted and caller-controlled.","setup.rb:87 - `require arg.split(/=/,2)[1]`: arbitrary Ruby file load from a command-line path.","setup.rb:322 - `eval_file_ifexist` runs `instance_eval File.read(fname)`: arbitrary Ruby execution from a tree file (`metaconfig`).","setup.rb:617 - `try_run_hook` runs `instance_eval File.read(fname)`: arbitrary Ruby execution from per-directory hook scripts found during traversal.","setup.rb:378 - `mkdir_p` creates each component of a concatenated `prefix + dirname` string, without normalization or symlink checks (setup.rb:392).","setup.rb:396 - `rm_f` does `File.chmod 0777, fname` then `File.unlink` (setup.rb:401-402); chmod follows symlinks.","setup.rb:406 - `rm_rf` recursive delete driven by `Dir.chdir`/`Dir.foreach` using `File.dir?` (stat, follows links) rather than lstat.","setup.rb:428 - `move_file` unlinks the destination then renames, falling back to read+write+`File.chmod File.stat(src).mode` (setup.rb:433-434).","setup.rb:439 - `install` writes content to `prefix + dest`, chmods it to the caller's mode (setup.rb:453), and appends the path to `InstalledFiles` (setup.rb:455).","setup.rb:1094 - `adjust_shebang` rewrites executables in place via a predictable temp file `File.basename(path) + '.tmp'` in the CWD (setup.rb:1096).","setup.rb:290 - `ConfigTable#save` writes the `.config` file that a later run re-reads and converts into command strings.","setup.rb:1225 - `_ruby_extentions` selects `.#{Config::CONFIG['DLEXT']}` files from the object dir for installation with mode 0555 (setup.rb:1148-1152).","tasks/vendor_sqlite3.rake:39 - `system \"wget -c #{url} || curl -C - -O #{url}\"`: shell command with interpolated URL; return value discarded so failure is silent.","tasks/vendor_sqlite3.rake:49 - second `system \"wget ... || curl ...\"` for the DLL archive; same unchecked shell download.","tasks/vendor_sqlite3.rake:59 - `sh \"unzip #{full_file}\"`: extraction of a network-downloaded zip into `vendor/sqlite3/include` with no entry-path validation or integrity check.","tasks/vendor_sqlite3.rake:75 - `sh \"unzip #{full_file}\"`: same for the DLL archive into `vendor/sqlite3/lib`.","tasks/vendor_sqlite3.rake:17 - `\"#{tool} --dllname #{dllname} --def #{deffile} --output-lib #{libfile}\"` returned into `sh` at tasks/vendor_sqlite3.rake:85.","tasks/vendor_sqlite3.rake:24 - MSVC branch `\"lib.exe /DEF:#{deffile} /OUT:#{libfile}\"`, binary resolved via PATH, executed through `sh`.","tasks/native.rake:28 - `sh \"swig -ruby -o #{t.name} #{t.prerequisites.first}\"`: code generation by an external PATH-resolved binary; the surrounding `rescue` swallows the real error.","tasks/native.rake:17 - `ext.config_options << \"--with-sqlite3-dir=#{sqlite3_lib}\"` (cross variant at tasks/native.rake:21): build-time library search path injected into the extconf invocation.","ext/sqlite3/extconf.rb:7 - compiler name from the environment is written into the generated Makefile (`create_makefile` at ext/sqlite3/extconf.rb:26), an execution sink for whoever later runs `make`.","ext/sqlite3/extconf.rb:19 - `find_header`/`find_library` (ext/sqlite3/extconf.rb:19-20) compile and link probe programs, executing the toolchain with caller-supplied paths.","Rakefile:18 - `spec_extras[:extensions] = [\"ext/sqlite3/extconf.rb\"]` declares the script RubyGems executes on every install of the packaged gem."],"assumptions":["setup.rb:472 - assumes every string reaching `command` is shell-safe; nothing quotes or escapes interpolated paths, config values, or `config-opt` arguments.","setup.rb:281 - assumes `.config` in the working directory is trustworthy and well-formed: no key whitelisting on read (unlike `[]=` at setup.rb:296-300), values only `strip`ped.","setup.rb:322 - assumes `metaconfig` and hook scripts inside the source tree were authored by the operator, so evaluating them as Ruby at full privilege is acceptable.","setup.rb:1073 - assumes `curr_srcdir()` (from `@ardir = File.expand_path(File.dirname($0))`, setup.rb:626-630) contains no shell metacharacters or spaces.","setup.rb:770 - assumes `--prefix` is benign; only `File.expand_path` is applied, with no containment check before mkdir/chmod/write beneath it.","setup.rb:400 - assumes the path being removed is not a symlink to something outside the tree; `chmod 0777` precedes the unlink and follows links.","setup.rb:1096 - assumes the current working directory is private, so a fixed-name `.tmp` file cannot be pre-created or symlinked by another user.","tasks/vendor_sqlite3.rake:36 - assumes the network path to www.sqlite.org is trustworthy: plain HTTP, no TLS, no checksum or signature verification before contents are compiled and shipped.","tasks/vendor_sqlite3.rake:59 - assumes the zip contains only expected relative paths; `unzip` is invoked without `-j` or any target restriction.","tasks/vendor_sqlite3.rake:39 - assumes a download failure will be noticed; the `system` result is discarded and the file task may then treat a partial or absent file as satisfied.","ext/sqlite3/extconf.rb:19 - assumes the sqlite3 header and library found on the search path are the intended ones; only presence of `sqlite3_libversion_number` is checked, with no minimum version.","Rakefile:26 - assumes `ENV['FAT_DIR']` is a simple directory name suitable as an output path component.","tasks/native.rake:7 - assumes `vendor/sqlite3` was already populated by the vendor task (it does not exist in a fresh checkout), so the compile step trusts a directory produced by the unverified download/unzip pipeline."],"trustBoundaries":["setup.rb:85 - command line to executed Ruby: `require` of an arbitrary path before any validation exists.","setup.rb:279 - on-disk `.config` text to in-process configuration that becomes argv[0] of shelled-out commands (`ruby-prog`, `make-prog`).","setup.rb:322 - files in the unpacked source tree to evaluated Ruby at the installer's privilege level, which for the `install` task is typically root; same boundary at setup.rb:617.","setup.rb:1072 - user-supplied `config-opt` strings to shell command line: data becomes command.","setup.rb:1160 - source-tree file list to writes and chmods under `bin-dir`/`rb-dir`/`so-dir`/`data-dir`, i.e. unprivileged tree content crossing into system directories.","tasks/vendor_sqlite3.rake:36 - cleartext network to local filesystem: an on-path, DNS, or HTTP hijack controls the bytes of both archives (no TLS, no hash pinning); also tasks/vendor_sqlite3.rake:46.","tasks/vendor_sqlite3.rake:55 - downloaded archive to build tree: unzip output lands in `vendor/sqlite3/{include,lib}`, which tasks/native.rake:17 and tasks/native.rake:21 then feed to the compiler and linker as trusted headers and libraries.","ext/sqlite3/extconf.rb:7 - process environment to generated Makefile: `ENV['CC']` becomes a command later executed by `make`.","Rakefile:18 - gem package to the installing user's machine: extconf.rb is executed by RubyGems on `gem install`, placing this component on the supply-chain trust boundary.","tasks/native.rake:26 - `.i` interface file in the tree to SWIG-generated C compiled into the extension; note `ext/sqlite3_api/` no longer exists, so this rule is stale."],"hotFiles":["setup.rb:1 - read in full (1333 lines). Vendored setup.rb 3.3.0 (c. 2004) containing every shell-out, both `instance_eval` of tree-resident scripts, all filesystem mutation helpers (mkdir_p, rm_f, rm_rf, move_file, install, adjust_shebang) and the whole ARGV/.config parsing surface. Highest-risk file in the component.","tasks/vendor_sqlite3.rake:1 - read in full (106 lines). Plaintext-HTTP download of a pinned but unverified SQLite 3.6.16 amalgamation and Windows DLL, shell `wget||curl`, unchecked `unzip`, dlltool/lib.exe command construction, and the hook making this a prerequisite of `compile`/`cross` at lines 100-106.","ext/sqlite3/extconf.rb:1 - read in full (26 lines). The script RubyGems executes on install: ENV-driven CC override, dir_config search paths, mkmf compile/link probes, Makefile generation.","tasks/native.rake:1 - read in full (35 lines). Wires the vendored (downloaded) sqlite3 directory into cross-compile config options, invokes swig via `sh`, and makes `compile` a prerequisite of `test`.","Rakefile:1 - read in full (32 lines). Gem spec declaring the extension, `ENV['FAT_DIR']` in the output path, and the Hoe plugin set; it does not itself load tasks/*.rake, so Hoe's task loading determines which of those files run."],"notes":"Read-only review: no build, test, execution, install, network access, or file modification was performed. `vendor/` does not exist in this checkout, so the download/unzip pipeline was assessed statically from tasks/vendor_sqlite3.rake. `ext/sqlite3_api/` referenced by tasks/native.rake:26-32 also does not exist (only ext/sqlite3/ is present), so that swig rule is dead code retained in the tree. Per stage instructions, no vulnerability judgements are recorded."}